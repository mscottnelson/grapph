language: node_js
node_js:
  - "stable"

services:
  - postgresql
  - redis-server

before_install:
  - psql -c 'create database grapph_test;' -U postgres

env:
  - CXX=g++-4.8

addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - g++-4.8
